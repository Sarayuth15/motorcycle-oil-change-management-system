package com.example.backend.enums;

public enum NotificationStatus {
    PENDING,            // Not yet due
    NOTIFIED,           // Email sent
    ACKNOWLEDGED        // User confirmed service done
}
